# 🚀 CertMaster 快速启动指南

本项目提供了多种启动方式，请根据您的操作系统选择合适的方法。

---

## 📋 启动前准备

### 1️⃣ 安装依赖

#### Windows
```bash
# 安装前端依赖
cd frontend
npm install

# 安装后端依赖
cd ../backend
pip install -r requirements.txt
```

#### Linux/Mac
```bash
# 安装前端依赖
cd frontend
npm install

# 安装后端依赖
cd ../backend
pip3 install -r requirements.txt
```

---

## 🎯 启动方式（选择一种）

### 方式 1：一键启动脚本（推荐）⭐

#### Windows 用户

**方法 A：双击批处理文件**
```
双击项目根目录下的 "启动.bat" 文件
```

**方法 B：PowerShell 脚本**
```powershell
.\start.ps1
```

#### Linux/Mac 用户
```bash
# 赋予执行权限（首次运行）
chmod +x start.sh

# 启动
./start.sh
```

#### 所有系统（使用 Python 脚本）
```bash
python scripts/start.py
```

---

### 方式 2：手动分别启动

#### 终端 1 - 启动后端
```bash
cd backend
pip install -r requirements.txt
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

#### 终端 2 - 启动前端
```bash
cd frontend
npm install
npm run dev
```

---

## 🌐 访问地址

启动成功后，访问以下地址：

- **前端界面**: http://localhost:5173
- **后端 API**: http://localhost:8000
- **API 文档**: http://localhost:8000/api/docs
- **API 交互**: http://localhost:8000/api/redoc

---

## 🛠️ 常见问题

### ❓ 端口被占用

如果提示端口被占用，可以：

**Windows:**
```bash
# 查看占用端口的进程
netstat -ano | findstr :8000
netstat -ano | findstr :5173

# 杀死进程（替换 PID）
taskkill /F /PID <进程ID>
```

**Linux/Mac:**
```bash
# 查看占用端口的进程
lsof -i :8000
lsof -i :5173

# 杀死进程（替换 PID）
kill -9 <进程ID>
```

---

### ❓ 依赖安装失败

**Node.js 依赖问题:**
```bash
cd frontend
rm -rf node_modules package-lock.json
npm install
```

**Python 依赖问题:**
```bash
cd backend
pip install --upgrade pip
pip install -r requirements.txt --force-reinstall
```

---

### ❓ 前端无法连接后端

1. 确保后端服务已启动（端口 8000）
2. 检查防火墙设置
3. 检查前端配置文件中的 API 地址

---

### ❓ 页面显示空白

1. 清除浏览器缓存（Ctrl+Shift+Delete）
2. 使用无痕模式打开
3. 检查浏览器控制台的错误信息（F12）

---

## 🔧 开发模式说明

启动脚本使用开发模式，具有以下特性：

- ✅ **热重载**: 修改代码自动刷新
- ✅ **详细日志**: 显示详细的错误信息
- ✅ **调试友好**: 支持断点调试
- ✅ **CORS 开启**: 允许跨域请求

---

## 🚀 生产部署

生产环境部署请参考：
- [DEPLOYMENT.md](docs/DEPLOYMENT.md) - 完整部署指南
- [START.md](docs/START.md) - 启动说明

---

## 📞 获取帮助

遇到问题？

1. 查看 [README.md](README.md)
2. 查看 [docs/](docs/) 目录下的文档
3. 检查终端输出的错误信息
4. 查看浏览器控制台（F12）

---

## 💡 提示

- **第一次启动**: 需要下载安装依赖，可能需要几分钟
- **后续启动**: 如果依赖已安装，启动速度会很快
- **停止服务**: 按 `Ctrl+C` 或关闭终端窗口

---

**🎉 祝您使用愉快！**

