# CertMaster 证书制作系统 - 部署指南

## 📋 目录
1. [环境要求](#环境要求)
2. [部署步骤](#部署步骤)
3. [启动服务](#启动服务)
4. [数据迁移](#数据迁移可选)
5. [常见问题](#常见问题)

---

## 🖥️ 环境要求

### 必需软件
- **Python 3.8+** - [下载地址](https://www.python.org/downloads/)
- **Node.js 16+** - [下载地址](https://nodejs.org/)
- **操作系统**：Windows 10/11（推荐）

### 硬件要求
- **内存**：至少 4GB RAM
- **硬盘**：至少 500MB 可用空间
- **浏览器**：Chrome、Edge 或 Firefox（最新版本）

---

## 📦 部署步骤

### 方法一：完整项目复制（推荐）

#### 1. 复制项目文件

将整个项目文件夹复制到目标电脑，例如：
```
D:\CertMaster\
```

**需要复制的文件夹和文件**：
```
CertMaster/
├── backend/           # 后端代码
├── frontend/          # 前端代码
├── data/             # 数据目录（包含证书、配置）
├── scripts/          # 脚本文件
├── 启动.bat          # 启动脚本（重要！）
└── README.md         # 说明文档
```

#### 2. 安装 Python

如果目标电脑没有 Python：

1. 下载 Python 3.8+ 安装包
2. 安装时**勾选** "Add Python to PATH"
3. 验证安装：打开命令提示符，输入：
   ```bash
   python --version
   ```

#### 3. 安装 Node.js

如果目标电脑没有 Node.js：

1. 下载 Node.js 16+ 安装包
2. 默认安装即可
3. 验证安装：
   ```bash
   node --version
   npm --version
   ```

#### 4. 安装依赖

**选项 A：自动安装（推荐）**

直接双击运行 `启动.bat`，脚本会自动：
- 检查环境
- 安装所有依赖
- 启动服务

**选项 B：手动安装**

打开命令提示符，进入项目目录：

```bash
# 进入项目目录
cd D:\CertMaster

# 安装后端依赖
cd backend
pip install -r requirements.txt
cd ..

# 安装前端依赖
cd frontend
npm install
cd ..
```

---

### 方法二：从源代码部署

#### 1. 克隆代码（如果有 Git）

```bash
git clone <repository-url>
cd CertMaster
```

#### 2. 按照方法一的步骤 2-4 操作

---

## 🚀 启动服务

### 方式一：使用启动脚本（最简单）

1. 双击运行 `启动.bat`
2. 等待两个窗口打开：
   - `CertMaster-Backend` - 后端服务
   - `CertMaster-Frontend` - 前端服务
3. 浏览器访问：http://localhost:5173

### 方式二：手动启动

**启动后端**（端口 8000）：
```bash
cd backend
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

**启动前端**（端口 5173）：
```bash
cd frontend
npm run dev
```

### 访问地址

- 🌐 **前端页面**：http://localhost:5173
- 🔧 **后端 API**：http://localhost:8000
- 📚 **API 文档**：http://localhost:8000/api/docs

---

## 📂 数据迁移（可选）

如果需要将旧电脑的数据迁移到新电脑：

### 1. 导出数据（在旧电脑上）

**方法一：直接复制数据文件夹**
```
复制整个 data/ 文件夹到新电脑的项目目录
```

**方法二：使用导出功能**
1. 打开系统：http://localhost:5173
2. 点击右上角 **设置图标** → **模板管理**
3. 点击 **导出配置** 按钮
4. 保存配置文件（如：`certmaster_configs_2025-10-03.json`）

### 2. 导入数据（在新电脑上）

**方法一：复制数据文件夹**
```
将 data/ 文件夹覆盖到新电脑的项目目录
```

**方法二：使用导入功能**
1. 打开系统
2. 进入 **模板管理**
3. 点击 **导入配置**
4. 选择之前导出的配置文件

### 3. 迁移的数据内容

复制 `data/` 文件夹会迁移：
- ✅ 所有证书数据 (`certificates.json`)
- ✅ 所有证书图片 (`certificates/` 文件夹)
- ✅ 模板配置 (`configs/` 文件夹)
- ✅ 用户上传的模板 (`templates.json`)

---

## ❓ 常见问题

### 1. 提示"未找到 Python"

**解决方案**：
- 确保已安装 Python 3.8+
- 重新安装 Python，勾选 "Add Python to PATH"
- 重启电脑后再试

### 2. 提示"未找到 Node.js"

**解决方案**：
- 确保已安装 Node.js 16+
- 重启命令提示符后再试
- 检查环境变量是否包含 Node.js 路径

### 3. 端口被占用

**错误信息**：`Address already in use: 8000` 或 `5173`

**解决方案**：
- 关闭占用端口的程序
- 或修改端口号：
  - 后端：编辑 `启动.bat`，将 8000 改为其他端口
  - 前端：编辑 `vite.config.ts`

### 4. 依赖安装失败

**解决方案**：
```bash
# Python 依赖
pip install -r requirements.txt --upgrade

# Node 依赖
cd frontend
npm install --legacy-peer-deps
```

### 5. 前端无法连接后端

**检查项**：
- ✅ 后端服务是否启动（访问 http://localhost:8000/api/health）
- ✅ 防火墙是否阻止
- ✅ API 地址配置是否正确（`frontend/src/utils/api.ts`）

### 6. 启动后页面空白

**解决方案**：
1. 按 F12 打开开发者工具
2. 查看 Console 标签页的错误信息
3. 刷新页面（Ctrl + F5）

### 7. 中文乱码问题

**解决方案**：
- 确保命令提示符使用 UTF-8 编码
- `启动.bat` 第2行已设置：`chcp 65001 >nul`

---

## 🔧 高级配置

### 修改端口号

**后端端口**（编辑 `backend/app/main.py`）：
```python
if __name__ == "__main__":
    import uvicorn
    uvicorn.run(app, host="0.0.0.0", port=8000)  # 修改这里
```

**前端端口**（编辑 `frontend/vite.config.ts`）：
```typescript
export default defineConfig({
  server: {
    port: 5173,  // 修改这里
  }
})
```

### 配置后端 API 地址

如果后端在其他电脑或端口，编辑 `frontend/src/utils/api.ts`：
```typescript
const API_BASE_URL = 'http://localhost:8000/api';  // 修改这里
```

---

## 📞 技术支持

如遇到问题：
1. 查看日志文件（服务窗口中的错误信息）
2. 检查 `data/` 文件夹权限
3. 确认防火墙设置

---

## 📝 部署清单

部署前请确认：

- [ ] Python 3.8+ 已安装
- [ ] Node.js 16+ 已安装  
- [ ] 项目文件已完整复制
- [ ] 依赖已安装（或运行启动脚本自动安装）
- [ ] 数据已迁移（如需要）
- [ ] 启动脚本可以运行
- [ ] 浏览器可以访问 http://localhost:5173

---

**部署完成后，即可开始使用 CertMaster 证书制作系统！** 🎉


