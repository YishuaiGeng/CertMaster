# 📋 证书模板配置说明

## ✅ 已完成的修改

### 1. 默认字体大小调整
- ✨ **默认字体大小**: 从 `40px` 调整为 `43px`
- ✨ **默认对齐方式**: 保持 `左对齐` (left)
- ⚠️ **例外字段**:
  - **姓名字段**: 默认 `48px` （比其他字段大一些）
  - **授权时间字段**: 默认 `右对齐` (right)（符合常规证书格式）

### 2. 配置保存和加载日志增强
添加了完整的日志追踪系统，包括：
- 🔧 字段配置变更日志
- 💾 保存配置详细日志
- ✅ 加载配置验证日志
- 🎨 渲染应用参数日志

---

## 🔍 如何验证配置是否正确保存

### 步骤 1：编辑模板配置

1. 打开系统，点击**"模板管理"**
2. 选择一个证书模板，点击**"编辑"**按钮
3. 在左侧选择**"授权时间"**字段
4. 修改对齐方式（例如：从右对齐改为左对齐）

**查看控制台（F12）**：
```
🔧 字段配置变更: authTime.align = left
   更新后的 authTime 配置: { x: 650, y: 650, align: 'left', ... }
```

### 步骤 2：保存配置

点击**"保存配置"**按钮

**查看控制台（F12）**：
```
💾 准备保存模板配置...
   模板名称: 通用技术学习证书
   完整配置: { ... }
   ✨ 授权时间配置: {
     x: 650,
     y: 650,
     align: 'left',  ← 确认对齐方式已修改
     fontSize: 43,
     ...
   }
✅ 配置已保存到 LocalStorage: template_config_xxx
   验证: 保存的数据长度: xxxx 字节
   验证: 读取授权时间配置: { align: 'left', ... }
```

### 步骤 3：重新加载验证

1. 关闭编辑器
2. 再次点击该模板的**"编辑"**按钮

**查看控制台（F12）**：
```
🔧 开始编辑模板: 通用技术学习证书
✅ 加载模板配置: 通用技术学习证书
   完整配置: { ... }
   ✨ 授权时间配置: {
     align: 'left',  ← 确认配置已保存并重新加载
     x: 650,
     y: 650,
     ...
   }
```

### 步骤 4：预览渲染验证

1. 返回主页面
2. 选择刚才配置的证书模板
3. 填写授权时间字段

**查看控制台（F12）**：
```
🎨 使用模板配置参数绘制
   授权时间配置: { align: 'left', ... }
   📝 绘制字段 授权时间: {
     text: '授权时间：二〇二五年十月一日',
     x: 650,
     y: 650,
     align: 'left',  ← 确认渲染时使用正确的对齐方式
     ...
   }
```

---

## 📊 默认配置一览表

| 字段 | X坐标 | Y坐标 | 字体大小 | 对齐方式 | 字体粗细 | 说明 |
|------|-------|-------|----------|----------|----------|------|
| **姓名** | 400 | 200 | 48px | 左对齐 | 普通 | 比其他字段大 |
| **证书内容** | 400 | 300 | 43px | 左对齐 | 普通 | 默认配置 |
| **指导单位** | 400 | 500 | 43px | 左对齐 | 普通 | 默认配置 |
| **证书编号** | 150 | 650 | 43px | 左对齐 | 普通 | 靠左下角 |
| **授权时间** | 650 | 650 | 43px | **右对齐** | 普通 | 靠右下角 |
| **授权单位** | 400 | 550 | 43px | 左对齐 | 普通 | 默认配置 |

---

## 🛠️ 可配置参数说明

### 文本字段参数

| 参数 | 类型 | 说明 |
|------|------|------|
| `x` | 数字 | X 坐标（根据对齐方式决定定位点） |
| `y` | 数字 | Y 坐标（文本基线位置） |
| `fontSize` | 数字 | 字体大小（像素） |
| `fontFamily` | 字符串 | 字体名称 |
| `fontWeight` | 'normal' \| 'bold' | 字体粗细 |
| `color` | 字符串 | 颜色（十六进制） |
| `align` | 'left' \| 'center' \| 'right' | 对齐方式 |
| `maxWidth` | 数字（可选） | 最大宽度，超出自动换行 |
| `maxHeight` | 数字（可选） | 最大高度限制 |

### 盖章参数

| 参数 | 类型 | 说明 |
|------|------|------|
| `x` | 数字 | 中心点 X 坐标 |
| `y` | 数字 | 中心点 Y 坐标 |
| `width` | 数字 | 宽度（像素） |
| `height` | 数字 | 高度（像素） |
| `rotation` | 数字 | 旋转角度（度，建议 -10 ~ +10） |

---

## 🎯 对齐方式定位点说明

### 左对齐 (left)
```
(x, y) ●————— 文本从这里开始
        Hello World
```

### 居中 (center)
```
        ●————— (x, y) 中心点
    Hello World
```

### 右对齐 (right)
```
        ——————● (x, y) 文本在这里结束
    Hello World
```

### Y 坐标说明
```
        Hello World
        ———————————— ← (y) 文本基线
```

---

## 💡 常见问题

### ❓ 修改配置后没有生效？

**检查清单：**

1. ✅ **确认已保存**: 点击了"保存配置"按钮
2. ✅ **检查控制台**: 看到"✅ 配置已保存"的提示
3. ✅ **刷新页面**: 有时需要刷新浏览器
4. ✅ **重新选择模板**: 在主页面重新选择证书模板
5. ✅ **查看日志**: 按 F12 打开控制台，查看详细日志

### ❓ 如何确认配置已保存到本地？

**方法 1：查看控制台日志**
```
✅ 配置已保存到 LocalStorage: template_config_xxx
   验证: 读取授权时间配置: { align: 'xxx', ... }
```

**方法 2：手动检查 LocalStorage**
1. 按 F12 打开开发者工具
2. 切换到 "Application" 或 "存储" 标签
3. 左侧选择 "Local Storage"
4. 查找 `template_config_` 开头的键

### ❓ 为什么授权时间默认是右对齐？

这是根据证书常规格式设计的：
- **证书编号**通常在左下角（左对齐）
- **授权时间**通常在右下角（右对齐）

您可以在模板编辑器中修改为其他对齐方式。

---

## 🧪 测试步骤

### 完整测试流程

1. **启动系统**
   ```bash
   python scripts/start.py
   ```

2. **打开浏览器控制台**（F12）

3. **编辑模板**
   - 点击"模板管理"
   - 选择证书模板，点击"编辑"
   - 修改授权时间的对齐方式
   - 观察控制台日志

4. **保存配置**
   - 点击"保存配置"
   - 确认看到成功提示
   - 检查控制台验证日志

5. **关闭并重新编辑**
   - 关闭编辑器
   - 再次编辑同一模板
   - 确认配置已正确加载

6. **预览测试**
   - 返回主页
   - 选择该证书模板
   - 填写字段内容
   - 查看预览效果
   - 确认对齐方式正确

---

## 📝 日志说明

### 🔧 配置变更日志
```
🔧 字段配置变更: [字段名].[参数] = [新值]
   更新后的 [字段名] 配置: { ... }
```

### 💾 保存日志
```
💾 准备保存模板配置...
   模板名称: [模板名]
   完整配置: { ... }
   ✨ 授权时间配置: { ... }
✅ 配置已保存到 LocalStorage: [键名]
   验证: 保存的数据长度: [字节]
   验证: 读取授权时间配置: { ... }
```

### 🔧 加载日志
```
🔧 开始编辑模板: [模板名]
✅ 加载模板配置: [模板名]
   完整配置: { ... }
   ✨ 授权时间配置: { ... }
```

### 🎨 渲染日志
```
🎨 使用模板配置参数绘制
   授权时间配置: { ... }
   📝 绘制字段 授权时间: { ... }
✅ 使用配置参数绘制完成
```

---

## 🎉 更新总结

### ✅ 已修复的问题

1. **默认字体大小**: 40px → 43px ✅
2. **配置保存追踪**: 添加详细日志 ✅
3. **配置加载验证**: 添加验证日志 ✅
4. **渲染应用确认**: 添加渲染日志 ✅

### 🔍 调试能力提升

现在您可以：
- ✅ 实时看到配置变更
- ✅ 确认配置已正确保存
- ✅ 验证配置已正确加载
- ✅ 确认渲染时使用正确参数

---

**🎓 现在您可以放心地修改和保存模板配置了！所有操作都有详细的日志追踪。**

